# Changelog

## 0.1.0

- created the requested application package structure
- added typed configuration and project persistence models
- added dataset validation and worker result parsing
- added a Qt Widgets main window with the requested navigation pages
- added setup, build, and installer scaffolding
- added CPU-safe pytest coverage for foundational behavior

